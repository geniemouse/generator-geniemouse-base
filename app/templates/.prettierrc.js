/**
 * Prettier rules
 * --------------
 * Individual key/values here overrule those from the shared "@geniemouse/prettier-config" package.
 *
 * If rule overriding not working, add ```...require("@geniemouse/prettier-config"),````
 * as first line inside the module.exports object.
 */

module.exports = {
    // e.g.:
    // semi: true
};
